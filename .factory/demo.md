# Demo sandbox

- URL: `https://math-prerequisite-sketch.sociobot.in/demo` (local: `http://localhost:5173/demo`)
- Sample: “Differentiate f(x) = (x + 3)²,” with a stopped line about the derivative step.
- State: demo data is held in memory. Demo mode never reads or writes the real `mmstep:sketch` local-storage key.
- Reset: choose **Reset demo** in the persistent amber banner.
- Leave: choose **Start for real**. The sample is discarded and the local workspace opens.
- Verification: every public claim can be exercised from `/demo` or a fresh local workspace without an account or network service.
